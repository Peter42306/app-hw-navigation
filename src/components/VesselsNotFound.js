const VesselsNotFound = () => {
    
    const VesselsNotFoundText = "404, the page is not found.";
    
    return(
        <div>
            <p>{VesselsNotFoundText}</p>
        </div>
    );
}

export default VesselsNotFound;